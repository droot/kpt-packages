apiVersion: networking.k8s.io/v1
kind: Ingress
metadata: # kpt-merge: example/ghost-app
  name: ghost-app
  namespace: example
  annotations:
    internal.kpt.dev/upstream-identifier: networking.k8s.io|Ingress|example|ghost-app
spec:
  rules:
    - host: ghost.local
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: ghost-app
                port:
                  name: http
